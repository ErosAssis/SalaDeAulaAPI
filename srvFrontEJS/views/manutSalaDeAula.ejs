<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Manutenção de Salas de Aula</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <header class="text-center mb-5">
            <h1 class="display-5">Manutenção de Salas de Aula</h1>
            <p class="text-muted">Gerencie as informações das salas cadastradas.</p>
        </header>

        <table class="table table-striped table-hover table-bordered">
            <thead class="table-dark">
                <tr class="text-center">
                    <th>ID</th>
                    <th>Descrição</th>
                    <th>Localização</th>
                    <th>Capacidade</th>
                </tr>
            </thead>
            <tbody>
                <% salas.forEach(sala => { %>
                    <tr>
                        <td class="text-center"><%= sala.id %></td>
                        <td><%= sala.descricao %></td>
                        <td><%= sala.localizacao %></td>
                        <td class="text-center"><%= sala.capacidade %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <div class="text-center mt-4">
            <a href="/" class="btn btn-secondary">Voltar para a Página Inicial</a>
        </div>
    </div>

    <footer class="text-center mt-5 py-3 bg-light">
        <p class="mb-0 text-muted">© 2024 Sistema ERP de Salas de Aula</p>
    </footer>
</body>
</html>
